// arrow function =>
// we can also use the function in sorting out of arrays 

let grades = [100, 50, 90, 60, 80, 70];

grades.sort((x, y) => x - y);
grades.forEach((element) => console.log(element));